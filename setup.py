"""Setup configuration for the web crawler package."""

# This file is kept for backward compatibility with older tools
# The project now uses pyproject.toml as the primary configuration

from setuptools import setup

setup()
