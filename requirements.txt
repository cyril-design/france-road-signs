# This file is kept for backward compatibility
# The project now uses pyproject.toml as the primary configuration
# See pyproject.toml for dependency management

requests==2.31.0
beautifulsoup4==4.12.2
python-dotenv==1.0.0
pytest==7.4.3
pytest-cov==4.1.0
lxml==4.9.3
